from typing import Any, Dict

def run_qaoa_gi(dataset: Dict[str, Any], config: Dict[str, Any]) -> Dict[str, Any]:
    """QAOA-based (sub)graph isomorphism. Implement in Weeks 4–5."""
    raise NotImplementedError("Week 4–5")
