from typing import Any, Dict

def run_gnn_baseline(dataset: Dict[str, Any], config: Dict[str, Any]) -> Dict[str, Any]:
    """Small GNN baseline (GCN/GIN). Keep torch import inside the future implementation."""
    raise NotImplementedError("Week 3")
